# Fake Secret Fixture

> **Fixture purpose**: triggers R2 (secret-pattern match). Contains a synthetic AWS-style key shape that matches the secret-pattern regex. The key value is non-functional.

```
aws_secret_access_key = "wJalrXUtnFEMIKEXAMPLEABCDEFGHIJKLMNOPQRST"
```

(Fixture-only synthetic value — NOT a real credential. The regex matches the *pattern shape*, not the specific bytes.)
